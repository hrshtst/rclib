{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *cpp_core/third_party/*) ;; *.py) .venv/bin/ruff format \"$f\"; .venv/bin/ruff check --fix \"$f\" ;; *.c|*.cc|*.cxx|*.cpp|*.h|*.hpp|*.hxx) clang-format -i \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting edited file"
          }
        ]
      }
    ]
  }
}
